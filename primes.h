/* primes.h
   prototypes for functions in primes.c
   prime(n) returns true if n is a prime (currently limited to 32-bit integers for reliable answers)
   nextPrime(n) returns next prime number >= n
*/

int prime(unsigned);
unsigned nextPrime(unsigned);
